# Plan 10 resume prompt

Paste the block below into a fresh Claude Code session to continue.

---

I'm building **Trivix** at `/Users/macblack/Downloads/Web Dev/Trivix` — a Next.js 16 + Firebase trivia app, on `main` branch, deployed to Netlify (`triviax.netlify.app`). Plans 1–9 are tagged complete. **Plan 10 (Presenter View + QR Join + Animated Reveals)** is in progress.

**Plan doc:** `docs/superpowers/plans/2026-05-07-plan-10-presenter-qr-reveals.md` — read this first. All 7 decisions (D1–D7) are locked at the recommended values.

**Status: 2/12 tasks done** (200, 201). Most recent commit: `2ed4ff4`.

**Next up — Task 202 + 203 + 204 (presenter view batch):**

1. **Task 202** — Create `app/(app)/host/games/[id]/present/page.tsx` as a server component. Same host-only auth check as `app/(app)/host/games/[id]/page.tsx` (verifySession, fetch session, redirect non-hosts to `/host`). Wrap a new client `PresenterView` component.

2. **Task 203** — Build `app/(app)/host/games/[id]/present/PresenterView.tsx`. Client component using the existing `useGameSession(sessionId, myUid)` hook from `hooks/useGameSession.ts`. Renders state-aware:
   - `lobby`: huge centered session code + QR code (use `<QrCode value={buildJoinUrl(window.location.origin, sessionCode)} size={384} />` from `components/games/QrCode.tsx`), live player count
   - `active`: massive question prompt at top, 4 choices in 2x2 grid, large `<Countdown>` from `components/games/Countdown.tsx`, correct-answer reveal styling
   - `ended`: final scoreboard — top 5 teams (use `aggregateTeams` from `lib/games/team-aggregate.ts`) + top 5 players, winner highlighted
   - Optimized for 10ft+ readability (use `font-display` at large sizes, `tracking-[8px]` for the session code)

3. **Task 204** — Add a smaller `<QrCode>` to `app/(app)/host/games/[id]/HostGameDashboard.tsx` lobby state, next to the existing session code display, plus a "Open presenter view" link to `/host/games/[id]/present` (open in new tab — `target="_blank"`).

**Project conventions you must follow** (from `CLAUDE.md` + the codebase pattern):
- Use `"use client"` for components with hooks; server components for auth-checked pages
- Use existing UI primitives from `components/ui/`: `Button`, `Card`, `Badge`, `Input`
- Tailwind classes only, theme tokens from `styles/theme.ts` (`brand-red`, `brand-ink`, `text-text-muted`, etc.)
- `font-display` is Anton, used for big tracking-spread headlines
- Client routes live under route groups: `(app)/...` (auth-gated via `app/(app)/layout.tsx`), `(auth)/...`, `(admin)/...`
- Run `npm run typecheck && npm run lint` after each task
- `react-hooks/set-state-in-effect` lint rule fires on `useEffect` setState calls — wrap in `/* eslint-disable react-hooks/set-state-in-effect */ ... /* eslint-enable */` if the pattern is intentional
- Commit after each task in the conventional-commit style with the standard `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer; use a `cat <<'EOF'` heredoc and avoid apostrophes in the message body (they break the shell quoting we use)
- Push after every commit so CI runs

Start with Task 202 (the server page wrapper — small, builds confidence the route compiles), then 203, then 204. Verify build + run E2E middleware redirect on `/host/games/<id>/present` after the batch. Then ask whether to keep going into Task 205 (animated reveals) or stop.
